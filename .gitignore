# ── OS junk ─────────────────────────────────────
.DS_Store
.localized
Thumbs.db
desktop.ini

# ── Redundant archives (폴더 버전이 같은 위치에 존재) ──
*.zip

# ── Editor / IDE ───────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── Temp / build ───────────────────────────────
*.tmp
*.bak
__pycache__/

# ── Local backups (작업자별 prefix, PR 머지 전 스냅샷) ──
# 협업 규칙: 백업 파일은 git 추적 X (로컬 보관만)
(skswn2fnf)*
(Kimminhye-fnf)*
